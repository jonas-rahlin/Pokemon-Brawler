Skpade plan för vilken ordning jag skulle göra allt.

GPT hjälp genom hela projektet. 

HTML BAS
JS BAS FUNKTION
STYLING
EXTRA UPPGIFT

Satte upp work enviroment.

Skapade Basic HTML template.

Skapade functionalitet för att hämta api.

Skapade BAS funktionalitet för pokemon class.

Skapade HTML template för dom DOM skapande i JS.
Gav till Chat GPT för konstruktion.
Skapade funktionalitet för DOM element skapande.

Behövde en paus från JS så började styleing tidigare.

Gjorde klart JS för del 1.

Startade styling för del2.

funktion för pokemon battle, vill separera display func och battle mekanik.


Hinder: id för selectorer som används, som ska matchas för att appenda rätt div
    hur trigga eventlistner genom javascript istälelt för i domen
    eventlistener active pokemon error, 


    felet är att funktionen körs innan pokemon arr har hunnit skapats.

    vill separera display func och battle mekanik.


    hade problem med multiplier, som gav orunda tal, valde därför att köra math.raound på totaldmg, eftersom jag ville följa instruktionerna och ancända multipliern.

    stora problem med dom display sekvensen av fighten när det kommer till scope och parametrar, beslutade mig
    till slut att storea varje attack i en array och skapa sekvensen utifårn den.
